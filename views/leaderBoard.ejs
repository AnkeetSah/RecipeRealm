<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Leaderboard</title>
      <link rel="icon" type="image/png" href="/images/appLogo/fav.png">
 

    <link rel="stylesheet" href="/css/header.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Reset and General Styling */
        body,
        h1,
        p,
        ul,
        li,
        a {
            margin: 0;
            padding: 0;
            list-style: none;
            text-decoration: none;
            box-sizing: border-box;
        }

        /* General body styling */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #ffffff;
        }

        /* Main content container */
        .main-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: calc(100vh - 70px);
            padding: 20px;
            margin-top: 70px;
        }

        /* Leaderboard container styling */
        .leaderboard-container {
            background: #1e1e1e;
            padding: 40px 30px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 850px;
            text-align: center;
            border: 1px solid #2a2a2a;
            position: relative;
            overflow: hidden;
        }

        h1 {
            margin-bottom: 30px;
            color: #00ff00;
            font-size: 32px;
            font-weight: 600;
            position: relative;
            letter-spacing: 0.5px;
        }

        .logo-container {
            margin-bottom: 25px;
            position: relative;
            z-index: 1;
        }

        .logo-container img {
            max-width: 120px;
            height: auto;
            filter: drop-shadow(0 0 10px rgba(0, 255, 0, 0.3));
        }

        .leaderboard {
            list-style-type: none;
            padding: 0;
            margin: 0;
            border-radius: 12px;
            max-height: 400px;
            overflow-y: auto;
            position: relative;
            z-index: 1;
        }

        /* Custom scrollbar */
        .leaderboard::-webkit-scrollbar {
            width: 6px;
        }

        .leaderboard::-webkit-scrollbar-track {
            background: #1a1a1a;
            border-radius: 4px;
        }

        .leaderboard::-webkit-scrollbar-thumb {
            background: #00cc00;
            border-radius: 4px;
        }

        .leaderboard li {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 18px 20px;
            margin-bottom: 10px;
            background: #252525;
            color: white;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 500;
            transition: all 0.3s ease;
            border-left: 4px solid transparent;
        }

        .leaderboard li:hover {
            background: #2a2a2a;
            border-left: 4px solid #00ff00;
            transform: translateX(5px);
        }

        .userPic {
            width: 42px;
            height: 42px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 15px;
            border: 2px solid #333;
            transition: all 0.3s ease;
        }

        .leaderboard li:hover .userPic {
            border-color: #00ff00;
        }

        .leaderboard li h4 {
            font-size: 16px;
            cursor: pointer;
            margin: 0;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .leaderboard li:hover h4 {
            color: #00ff00;
        }

        .profile-link {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 36px;
            height: 36px;
            background: #333;
            border-radius: 50%;
            margin-left: 15px;
            transition: all 0.3s ease;
        }

        .profile-link:hover {
            background: #00cc00;
            transform: scale(1.1);
        }

        .profile-link i {
            color: #fff;
            font-size: 16px;
        }

        .rank {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 32px;
            height: 32px;
            background: #333;
            border-radius: 50%;
            text-align: center;
            margin-right: 12px;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .leaderboard li:hover .rank {
            background: #00cc00;
        }

        .top-3 .rank {
            width: 38px;
            height: 38px;
            font-size: 16px;
        }

        /* Special styling for top 3 */
        .leaderboard li:nth-child(1) {
            background: linear-gradient(to right, #252525, #2a2a2a);
            border-left: 4px solid #FFD700;
        }

        .leaderboard li:nth-child(1) .rank {
            background: #FFD700;
            color: #000;
        }

        .leaderboard li:nth-child(2) {
            background: linear-gradient(to right, #252525, #2a2a2a);
            border-left: 4px solid #C0C0C0;
        }

        .leaderboard li:nth-child(2) .rank {
            background: #C0C0C0;
            color: #000;
        }

        .leaderboard li:nth-child(3) {
            background: linear-gradient(to right, #252525, #2a2a2a);
            border-left: 4px solid #CD853F;
        }

        .leaderboard li:nth-child(3) .rank {
            background: #CD853F;
            color: #000;
        }

        .likes-count {
            display: flex;
            align-items: center;
            font-weight: 600;
            background: #333;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .leaderboard li:hover .likes-count {
            background: #00cc00;
        }

        .user-info {
            display: flex;
            align-items: center;
        }

        .user-stats {
            display: flex;
            align-items: center;
        }

        /* Animation for list items */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .leaderboard li {
            animation: fadeInUp 0.5s ease forwards;
            opacity: 0;
        }

        .leaderboard li:nth-child(1) {
            animation-delay: 0.1s;
        }

        .leaderboard li:nth-child(2) {
            animation-delay: 0.2s;
        }

        .leaderboard li:nth-child(3) {
            animation-delay: 0.3s;
        }

        .leaderboard li:nth-child(4) {
            animation-delay: 0.4s;
        }

        .leaderboard li:nth-child(5) {
            animation-delay: 0.5s;
        }

        .leaderboard li:nth-child(6) {
            animation-delay: 0.6s;
        }

        .leaderboard li:nth-child(7) {
            animation-delay: 0.7s;
        }

        .leaderboard li:nth-child(8) {
            animation-delay: 0.8s;
        }

        .leaderboard li:nth-child(9) {
            animation-delay: 0.9s;
        }

        .leaderboard li:nth-child(10) {
            animation-delay: 1.0s;
        }

        /* Stats section */
        .stats-container {
            display: flex;
            justify-content: space-between;
            margin-top: 30px;
            padding: 20px;
            background: #252525;
            border-radius: 12px;
        }

        .stat-item {
            text-align: center;
            flex: 1;
        }

        .stat-value {
            font-size: 24px;
            font-weight: 700;
            color: #00ff00;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 14px;
            color: #aaa;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .main-container {
                padding: 15px;
                margin-top: 70px;
            }

            .leaderboard-container {
                width: 90%;
                padding: 25px 15px;
            }

            h1 {
                font-size: 26px;
            }

            .leaderboard li {
                font-size: 14px;
                padding: 15px;
            }

            .userPic {
                width: 36px;
                height: 36px;
                margin-right: 10px;
            }

            .rank {
                width: 28px;
                height: 28px;
                margin-right: 10px;
                font-size: 12px;
            }

            .top-3 .rank {
                width: 32px;
                height: 32px;
                font-size: 14px;
            }

            .likes-count {
                font-size: 13px;
                padding: 6px 12px;
            }

            .stats-container {
                flex-direction: column;
                gap: 15px;
            }
        }

        @media (max-width: 480px) {
            .leaderboard-container {
                width: 95%;
                padding: 20px 10px;
            }

            h1 {
                font-size: 22px;
            }

            .leaderboard li {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }

            .user-stats {
                align-self: flex-end;
            }

            .user-info,
            .user-stats {
                width: 100%;
                justify-content: space-between;
            }
        }

        header img {
            height: 50px;
            position: relative;
        }
    </style>
</head>

<body>
    <!-- Header -->
    <%- include("./header") %>

        <div class="main-container">
            <div class="leaderboard-container">
                <div class="logo-container">
                    <img src="https://images.squarespace-cdn.com/content/v1/60576b36c740e90bc9edbc76/1617285138250-KO51124U9UURCJDG6EMF/Logo+transparent+background.png"
                        alt="Logo">
                </div>
                <h1>Top Contributors</h1>

                <ul id="leaderboard" class="leaderboard">
                    <% leaderBoard.forEach((user, i)=> { %>

                        <li class="<%= i < 3 ? 'top-3' : '' %>">
                            <div class="user-info">
                                <span class="rank">
                                    <%= i + 1 %>
                                </span>
                                
                                <img src="<%= user.pic %>" class="userPic" alt="Profile Picture">
                                <h4>
                                    <%= user.name %>
                                </h4>
                            </div>
                            <div class="user-stats">
                                <span class="likes-count">
                                    <%= user.total_likes %> Likes
                                </span>
                                <a href="/viewProfile/<%=user.id %>" class="profile-link">
                                    <i class="fas fa-external-link-alt"></i>
                                </a>
                            </div>
                        </li>
                        <% }) %>
                </ul>

                <div class="stats-container">
                    <div class="stat-item">
                        <div class="stat-value">
                            <%= leaderBoard.length %>
                        </div>
                        <div class="stat-label">Total Contributors</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value">
                            <%= leaderBoard[0] ? leaderBoard[0].total_likes : 0 %>
                        </div>
                        <div class="stat-label">Top Contributor Likes</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value">
                            <%= Math.round(leaderBoard.reduce((sum, user)=> sum + user.total_likes, 0) /
                                leaderBoard.length) || 0 %>
                        </div>
                        <div class="stat-label">Average Likes</div>
                    </div>
                </div>
            </div>
        </div>
        <script>
           
        </script>
</body>

</html>