<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Added</title>
    <link rel="icon" type="image/png" href="/images/appLogo/fav.png">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body, h1, p, ul, li, a {
            margin: 0;
            padding: 0;
            list-style: none;
            text-decoration: none;
            box-sizing: border-box;
        }
        *{
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #ffffff;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            padding: 2rem 80px;
            margin-top: 70px;
        }

        .post-container {
            flex: 1;
            padding: 1.5rem;
            background-color: #1f1f1f;
            border-radius: 12px;
            margin-right: 2rem;
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .post-container h2 {
            color: #00ff00;
        }

        .post-container p {
            color: #bfbfbf;
        }

        .sidebar {
            width: 200px;
            background-color: #1f1f1f;
            border-radius: 8px;
            padding: 1rem;
        }

        .sidebar a {
            display: block;
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: none;
            font-weight: bolder;
            border-radius: 4px;
            background-color: #00ff00;
            color: #000000;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
        }

        .sidebar a:hover {
            background-color: #00cc00;
        }

        .post {
            min-height: 250px;
            width: 280px; /* Increased width to accommodate all buttons */
            background: linear-gradient(145deg, #1a1a1a, #000000);
            border-radius: 12px;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 1rem;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.4);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: 1px solid #2a2a2a;
        }


        /* Media Queries for Responsive Design */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                padding: 20px 10px;
                
            }

            

            .post-container {
                margin-right: 0;
                margin-bottom: 2rem;
            }

            .sidebar {
                width: 100%;
                padding: 0.5rem;
            }

            .sidebar a {
                padding: 0.75rem;
                margin-bottom: 1rem;
            }
            
        }

        @media (max-width: 480px) {
            .navbar h1 {
                font-size: 1.5rem;
            }

            .post-container h2 {
                font-size: 1.25rem;
            }

            .post-container p {
                font-size: 0.9rem;
            }

            .sidebar a {
                font-size: 0.9rem;
                padding: 0.5rem;
            }

            .post-container{
                justify-content: space-around;
                overflow-y: auto;
                height: auto;
                margin-left: 0;
                flex-wrap: wrap;
                width: 100%;
            }
            .sidebar{
                margin-left: 0;
            }

            .post{
                width: 100%;
            }
            
        }

        /* ENHANCED CARD STYLES */
        
        .post::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #32CD32, #00cc00);
        }

        .post img {
            width: 100%;
            height: 130px;
            border-radius: 8px;
            object-fit: cover;
            margin-bottom: 0.75rem;
            transition: transform 0.3s ease;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .post:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 25px rgba(0, 0, 0, 0.5);
            border-color: #32CD32;
        }

        .post:hover img {
            transform: scale(1.05);
        }

        .post p {
            color: #ffffff;
            font-size: 1rem;
            text-align: center;
            margin: 0.5rem 0;
            font-weight: 600;
            width: 100%;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .post .meal-name {
            color: #32CD32;
            font-size: 1.1rem;
            margin-bottom: 0.75rem;
        }

        .action {
            display: flex;
            align-items: center;
            justify-content: space-between;
            width: 100%;
            margin-top: auto;
            padding-top: 0.75rem;
            border-top: 1px solid #2a2a2a;
            gap: 10px; /* Added gap between like count and buttons */
        }

        /* FIXED LIKE HEART STYLING */
        .likes {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 6px 10px;
            background: rgba(255, 107, 107, 0.15);
            border-radius: 20px;
            border: 1px solid rgba(255, 107, 107, 0.3);
            transition: all 0.3s ease;
            flex-shrink: 0; /* Prevent shrinking */
        }

        .likes:hover {
            background: rgba(255, 107, 107, 0.25);
            transform: scale(1.05);
        }

        .likes i {
            color: #ff6b6b;
            font-size: 0.9rem;
            text-shadow: 0 0 8px rgba(255, 107, 107, 0.5);
        }

        .likes span {
            color: #ffffff;
            font-size: 0.85rem;
            font-weight: 600;
            min-width: 16px;
            text-align: center;
        }

        .action-buttons {
            display: flex;
            gap: 6px;
            flex-wrap: nowrap; /* Prevent wrapping */
            justify-content: flex-end;
            flex-grow: 1; /* Take available space */
        }

        /* ENHANCED BUTTON STYLES - COMPACT VERSION */
        .action a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.4rem 0.6rem;
            border-radius: 6px;
            font-size: 0.7rem;
            font-weight: 600;
            transition: all 0.3s ease;
            text-decoration: none;
            border: none;
            cursor: pointer;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            min-width: 40px;
            gap: 3px;
            white-space: nowrap;
        }

        .action a.edit-btn {
            background: linear-gradient(135deg, #4CAF50, #45a049);
            color: white;
            border: 1px solid #3d8b40;
        }

        .action a.edit-btn:hover {
            background: linear-gradient(135deg, #45a049, #3d8b40);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(76, 175, 80, 0.3);
        }

        .action a.delete-btn {
            background: linear-gradient(135deg, #ff4757, #ff3742);
            color: white;
            border: 1px solid #e84118;
        }

        .action a.delete-btn:hover {
            background: linear-gradient(135deg, #ff3742, #e84118);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(255, 71, 87, 0.3);
        }

        .action a.view-btn {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            border: 1px solid #2573a7;
        }

        .action a.view-btn:hover {
            background: linear-gradient(135deg, #2980b9, #2573a7);
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(52, 152, 219, 0.3);
        }

        /* Button icons - smaller for compact layout */
        .action a i {
            font-size: 0.65rem;
        }

        header img {
            height: 50px;
            position: relative;
        }

        /* Empty state styling */
        .empty-state {
            width: 100%;
            text-align: center;
            padding: 2rem;
            color: #888;
        }

        .empty-state p {
            margin-bottom: 1rem;
        }

        .empty-state a {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background-color: #32CD32;
            color: #000;
            border-radius: 6px;
            font-weight: 600;
        }
    </style>
</head>
<body>
   <%- include("./header") %>
    <div class="container">
        <div class="post-container">
            <% if (posts.length > 0) { %>
                <% posts.forEach(post=>{ %>
                    <div class="post">
                        <img src="<%= post.strMealThumb%>" alt="<%= post.strMeal%>">
                        <p class="meal-name"><%= post.strMeal%></p>
                        <div class="action">
                            <div class="likes">
                                <i class="fas fa-heart"></i>
                                <span><%= post.total_likes.length  %></span>
                            </div>
                            <div class="action-buttons">
                                <a href="/account/postEdit/<%= post._id %>" class="edit-btn">
                                    <i class="fas fa-edit"></i> Edit
                                </a>
                                <a href="/account/postDelete/<%= post._id %>" class="delete-btn">
                                    <i class="fas fa-trash"></i> Del
                                </a>
                                <a href="/home/recipe/item/<%= post._id %>" class="view-btn">
                                    <i class="fas fa-eye"></i> View
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="empty-state">
                    <p>You haven't created any posts yet.</p>
                    <a href="/account/addDish">Create Your First Post</a>
                </div>
            <% } %>
        </div>
        <div class="sidebar">
            <a href="/account/addDish">Add Dish</a>
            <a href="/account/userSavedRecipe">Saved Posts</a>
            <a href="/account/postcreated" style="background-color: #000;color:white;border: 1px solid #00cc00;">Created Posts</a>
            <a href="/account/logout">Log out</a>
        </div>
    </div>

   
</body>
</html>