<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Dish</title>
    <link rel="icon" type="image/png" href="/images/appLogo/fav.png">

    <link rel="stylesheet" href="/css/header.css">
    <style>
        body,
        h1,
        p,
        ul,
        li,
        a {
            margin: 0;
            padding: 0;
            list-style: none;
            text-decoration: none;
            box-sizing: border-box;
        }

        /* Universal box-sizing fix */
        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }


        body {
            margin: 0;
            font-family: 'Segoe UI', system-ui, sans-serif;
            background-color: #121212;
            color: #ffffff;
        }



        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .menu-icon {
                display: block;
                font-size: 24px;
                cursor: pointer;
                color: #fff;
            }

            .menu-icon:hover {
                color: #32CD32;
            }

            .nav-links.mobile {
                display: flex;
                flex-direction: column;
                gap: 10px;
                background: #000;
                position: absolute;
                top: 60px;
                right: 20px;
                padding: 20px;
                border-radius: 5px;
            }

            .nav-links.mobile a {
                color: #32CD32;
            }
        }

        @media (min-width: 769px) {
            .menu-icon {
                display: none;
            }
        }

        .container {
            display: flex;
            margin-top: 70px;
            padding: 2rem 80px;
        }

        /* STUNNING FORM CONTAINER ENHANCEMENT */
        .form-container {
            flex: 1;
            padding: 2.5rem;
            background: linear-gradient(145deg, #1a1a1a 0%, #222 100%);
            border-radius: 20px;
            margin-right: 2rem;
            box-shadow:
                0 10px 40px rgba(0, 0, 0, 0.6),
                0 0 0 1px rgba(32, 248, 3, 0.1),
                inset 0 1px 0 rgba(255, 255, 255, 0.05);
            position: relative;
            overflow: hidden;
        }

        .form-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #20f803, #00ff00, #20f803);
            background-size: 200% 100%;
            animation: shimmer 3s infinite linear;
        }

        @keyframes shimmer {
            0% {
                background-position: -200% 0;
            }

            100% {
                background-position: 200% 0;
            }
        }

        .form-container h2 {
            color: #20f803;
            font-size: 2.2rem;
            margin-bottom: 2rem;
            text-align: center;
            font-weight: 700;
            text-shadow: 0 0 20px rgba(32, 248, 3, 0.4);
            letter-spacing: -0.5px;
            position: relative;
        }

        .form-container h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 3px;
            background: linear-gradient(90deg, transparent, #20f803, transparent);
            border-radius: 2px;
        }

        .form-group {
            margin-bottom: 1.8rem;
            position: relative;
        }

        .form-container label {
            display: block;
            margin-bottom: 0.8rem;
            color: #e8e8e8;
            font-weight: 600;
            font-size: 1rem;
            letter-spacing: 0.3px;
        }

        .form-container input,
        .form-container textarea {
            width: 100%;
            padding: 1rem 1.2rem;
            border: 2px solid #333;
            border-radius: 12px;
            background: linear-gradient(135deg, #2b2b2b 0%, #252525 100%);
            color: #ffffff;
            font-size: 1rem;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            font-family: inherit;
            box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .form-container input:focus,
        .form-container textarea:focus {
            outline: none;
            border-color: #20f803;
            box-shadow:
                0 0 0 3px rgba(32, 248, 3, 0.15),
                inset 0 2px 4px rgba(0, 0, 0, 0.4);
            background: linear-gradient(135deg, #2f2f2f 0%, #292929 100%);
            transform: translateY(-2px);
        }

        .form-container input::placeholder,
        .form-container textarea::placeholder {
            color: #888;
            opacity: 0.8;
        }

        .form-container textarea {
            resize: vertical;
            min-height: 120px;
            line-height: 1.5;
        }

        .form-hint {
            display: block;
            font-size: 0.85rem;
            color: #20f803;
            margin-top: 0.5rem;
            font-style: italic;
            opacity: 0.8;
            padding-left: 0.5rem;
            border-left: 2px solid #20f803;
        }

        .file-input-wrapper {
            position: relative;
            overflow: hidden;
            border-radius: 12px;
        }

        .file-input-wrapper input[type="file"] {
            position: absolute;
            left: 0;
            top: 0;
            opacity: 0;
            width: 100%;
            height: 100%;
            cursor: pointer;
        }

        .file-input-label {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.8rem;
            padding: 1.2rem;
            background: linear-gradient(135deg, #2b2b2b 0%, #252525 100%);
            border: 2px dashed #444;
            border-radius: 12px;
            color: #bfbfbf;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            font-weight: 500;
        }

        .file-input-label:hover {
            border-color: #20f803;
            color: #20f803;
            background: linear-gradient(135deg, #2f2f2f 0%, #292929 100%);
            transform: translateY(-2px);
        }

        .file-input-label.has-file {
            border-color: #20f803;
            color: #20f803;
            border-style: solid;
            background: rgba(32, 248, 3, 0.05);
        }

        .submit-btn {
            width: 100%;
            padding: 1.2rem 2rem;
            border: none;
            border-radius: 12px;
            background: linear-gradient(135deg, #20f803 0%, #1bd002 100%);
            color: #000;
            font-weight: 700;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            margin-top: 1rem;
            box-shadow:
                0 8px 25px rgba(32, 248, 3, 0.3),
                0 2px 0 rgba(255, 255, 255, 0.1) inset;
            position: relative;
            overflow: hidden;
            letter-spacing: 0.5px;
        }

        .submit-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s;
        }

        .submit-btn:hover {
            background: linear-gradient(135deg, #1bd002 0%, #18b802 100%);
            transform: translateY(-3px);
            box-shadow:
                0 12px 30px rgba(32, 248, 3, 0.4),
                0 2px 0 rgba(255, 255, 255, 0.1) inset;
        }

        .submit-btn:hover::before {
            left: 100%;
        }

        .submit-btn:active {
            transform: translateY(-1px);
        }

        .sidebar {
            width: 200px;
            background-color: #1f1f1f;
            border-radius: 8px;
            padding: 1rem;
        }

        .sidebar a,
        .sidebar button {
            display: block;
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: none;
            font-weight: bolder;
            border-radius: 4px;
            background-color: #00ff00;
            color: #000000;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
        }

        .sidebar a:hover,
        .sidebar button:hover {
            background-color: #00cc00;
        }

        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                padding: 1rem;
            }

            .form-container {
                margin-right: 0;
                margin-bottom: 2rem;
                margin-top: 20px;
                padding: 2rem;
            }

            .sidebar {
                width: 96%;
                padding: 0.5rem;
            }

            .sidebar a,
            .sidebar button {
                padding: 0.75rem;
                margin-bottom: 1rem;
            }
        }

        @media (max-width: 480px) {
            .form-container h2 {
                font-size: 1.25rem;
            }

            .sidebar a,
            .sidebar button {
                font-size: 0.9rem;
                padding: 0.5rem;
            }
        }

        /* Popup styles */
        .popup {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            padding: 20px;
            background-color: #000;
            color: #00ff00;
            border: 2px solid #00ff00;
            border-radius: 8px;
            z-index: 1000;
            text-align: center;
        }

        .popup button {
            margin-top: 10px;
            padding: 5px 10px;
            background-color: #00ff00;
            color: #000;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .popup button:hover {
            background-color: #00cc00;
        }

        .popup-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 999;
        }

        header img {
            height: 50px;
        }
    </style>
</head>

<body>
    <%- include("./header") %>
        <div class="container">
            <div class="form-container">
                <h2>Create a Dish Post</h2>
                <form id="dishForm" action="/account/dishUpload" method="post" enctype="multipart/form-data">
                    <div class="form-group">
                        <label for="dishName">Dish Name:</label>
                        <input type="text" id="dishName" name="dishName" required
                            placeholder="Enter your delicious dish name">
                    </div>

                    <div class="form-group">
                        <label for="dishCategory">Dish Category:</label>
                        <input type="text" id="dishCategory" name="dishCategory" required
                            placeholder="e.g., Italian, Asian, Dessert">
                        <span class="form-hint">Based on main ingredients, separated by commas</span>
                    </div>

                    <div class="form-group">
                        <label for="dishIngredients">Dish Ingredients:</label>
                        <textarea id="dishIngredients" name="dishIngredients" rows="4" required
                            placeholder="List all ingredients separated by commas"></textarea>
                        <span class="form-hint">Separate ingredients with commas</span>
                    </div>

                    <div class="form-group">
                        <label for="dishInstructions">Dish Instructions:</label>
                        <textarea id="dishInstructions" name="dishInstructions" rows="4" required
                            placeholder="Step-by-step cooking instructions"></textarea>
                        <span class="form-hint">Write clear instructions for preparation</span>
                    </div>

                    <div class="form-group">
                        <label for="dishDescription">Dish Description:</label>
                        <textarea id="dishDescription" name="dishDescription" rows="4" required
                            placeholder="Describe the flavors, texture, and special features"></textarea>
                    </div>

                    <div class="form-group">
                        <label for="dishYouTubeLink">Dish YouTube Link:</label>
                        <input type="url" id="dishYouTubeLink" name="dishYouTubeLink" required
                            placeholder="https://youtube.com/your-video">
                    </div>

                    <div class="form-group">
                        <label for="dishImage">Upload Dish Image:</label>
                        <div class="file-input-wrapper">
                            <input type="file" id="dishImage" name="dishImage" accept="image/*" required>
                            <div class="file-input-label" id="fileInputLabel">
                                📷 Choose Dish Image
                            </div>
                        </div>
                    </div>

                    <button class="submit-btn" type="submit">✨ Add Dish</button>
                </form>
            </div>
            <div class="sidebar">
                <a href="/account/addDish" style="background-color: #000;color:white;border: 1px solid #00cc00;">Add Dish</a>
                <a href="/account/userSavedRecipe">Saved Post</a>
                <a href="/account/postcreated">Dish Created</a>
                <a href="/account/logout">Log out</a>
            </div>
        </div>

        <div class="popup-overlay"></div>
        <div class="popup" id="popupMessage">
            <p>The dish has been added!</p>
            <button onclick="closePopup()">OK</button>
        </div>

        <script>
            

            const dishForm = document.getElementById('dishForm');
            const popup = document.getElementById('popupMessage');
            const overlay = document.querySelector('.popup-overlay');
            const fileInput = document.getElementById('dishImage');
            const fileInputLabel = document.getElementById('fileInputLabel');

            // Enhanced file input functionality
            fileInput.addEventListener('change', function () {
                if (this.files.length > 0) {
                    fileInputLabel.textContent = `📷 ${this.files[0].name}`;
                    fileInputLabel.classList.add('has-file');
                } else {
                    fileInputLabel.textContent = '📷 Choose Dish Image';
                    fileInputLabel.classList.remove('has-file');
                }
            });

            dishForm.addEventListener('submit', function (event) {
                showPopup();
            });

            function showPopup() {
                popup.style.display = 'block';
                overlay.style.display = 'block';
            }

            function closePopup() {
                popup.style.display = 'none';
                overlay.style.display = 'none';
            }
        </script>
</body>

</html>