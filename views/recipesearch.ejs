<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recipe App - Food List</title>
  <link rel="icon" type="image/png" href="./images/appLogo/fav.png">
  <link rel="stylesheet" href="/css/header.css">
  <style>
    /* Global Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      color: #eaeaea;
      background: linear-gradient(180deg, #0a0a0a, #1a1a1a);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .container {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 60px 20px;
    }

    h1 {
      font-size: 3rem;
      font-weight: 700;
      text-align: center;
      background: linear-gradient(90deg, #39ff14, #ff8c42);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 60px;
    }

    /* Card Grid */
    .card-wrapper {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 40px;
      width: 100%;
      max-width: 1000px;
    }

    /* Card */
    .card {
      background: #111;
      border-radius: 20px;
      padding: 40px 30px;
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease, border 0.3s ease;
      box-shadow: 0 0 15px rgba(57, 255, 20, 0.15);
      border: 1px solid transparent;
    }

    .card:hover {
      transform: translateY(-6px) scale(1.02);
      box-shadow: 0 0 25px rgba(57, 255, 20, 0.35);
      border: 1px solid rgba(57, 255, 20, 0.5);
    }

    .icon {
      font-size: 3rem;
      margin-bottom: 20px;
      display: inline-block;
    }

    .card h2 {
      font-size: 1.6rem;
      margin-bottom: 15px;
      font-weight: 600;
      color: #fff;
    }

    .card p {
      font-size: 1rem;
      margin-bottom: 25px;
      color: #bbb;
      line-height: 1.6;
    }

    /* Green Card */
    .card.green .icon {
      color: #39ff14;
    }

    .card.green a {
      background: #39ff14;
      color: #000;
    }

    .card.green a:hover {
      background: #52ff42;
      box-shadow: 0 0 15px rgba(57, 255, 20, 0.6);
    }

    /* Orange Card */
    .card.orange .icon {
      color: #ff8c42;
    }

    .card.orange a {
      background: #ff8c42;
      color: #fff;
    }

    .card.orange a:hover {
      background: #ffa45c;
      box-shadow: 0 0 15px rgba(255, 140, 66, 0.6);
    }

    /* Buttons */
    .card a {
      display: inline-block;
      padding: 12px 28px;
      text-decoration: none;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1rem;
      transition: background 0.3s ease, transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card a:hover {
      transform: scale(1.07);
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 20px;
      font-size: 0.85rem;
      background: #0f0f0f;
      color: #777;
      border-top: 1px solid #222;
      letter-spacing: 0.3px;
    }
  </style>
</head>

<body>
  <%- include("./header") %>

  <div class="container">
    <h1>Find Your Perfect Recipe</h1>

    <div class="card-wrapper">
      <!-- Card 1 -->
      <div class="card green">
        <div class="icon">🍅</div>
        <h2>Search by Single Ingredient</h2>
        <p>Enter one ingredient and instantly find recipes curated by our community. Perfect for when you have one
          key ingredient at home.</p>
        <a href="/home/recipe/single-recipe">Search Now</a>
      </div>

      <!-- Card 2 -->
      <div class="card orange">
        <div class="icon">🤖</div>
        <h2>AI Multi-Ingredient Search</h2>
        <p>Input multiple ingredients and let our AI suggest smart, creative dishes tailored to your pantry and taste
          preferences.</p>
        <a href="/search-multi">Try AI Search</a>
      </div>
    </div>
  </div>

  <footer>
    © 2025 RecipeRealm · Cook Smart, Eat Better
  </footer>
</body>

</html>
