<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
       <link rel="icon" type="image/png" href="./images/appLogo/fav.png">
       <link rel="stylesheet" href="/css/header.css">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #ffffff;
        }

        .navbar {
            background-color: black;
            padding: 6px 7px;
           
        }

        .navbar h1 {
            margin: 0;
            color: #00ff00;
        }

        .container {
            display: flex;
            padding: 2rem;
            
        }

        .form-container {
            flex: 1;
            padding: 1rem;
            background-color: #1f1f1f;
            border-radius: 8px;
            margin-right: 2rem;
        }

        .form-container h2 {
            color: #00ff00;
        }

        .form-container label {
            display: block;
            margin-bottom: 0.5rem;
            color: #bfbfbf;
        }

        .form-container input,
        .form-container textarea {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: none;
            border-radius: 4px;
            background-color: #2b2b2b;
            color: #ffffff;
        }

        .form-container button {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 4px;
            background-color: #00ff00;
            color: #000000;
            cursor: pointer;
        }

        .form-container button:hover {
            background-color: #00cc00;
        }

        .sidebar {
            width: 200px;
            background-color: #1f1f1f;
            border-radius: 8px;
            padding: 1rem;
        }

        .sidebar button {
            display: block;
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: none;
            border-radius: 4px;
            background-color: #00ff00;
            color: #000000;
            cursor: pointer;
        }

        .sidebar button:hover {
            background-color: #00cc00;
        }

        .current-image {
            margin-bottom: 1rem;
            text-align: center;
        }

        .current-image img {
            max-width: 100%;
            border-radius: 8px;
        }
        header img{
            height: 50px;
            position: relative;
        
        }
        @media(max-width:450px) {
            .container{
                flex-wrap: wrap;
            }
            .form-container{
                margin-right: 0px;
                margin-bottom: 10px;
            }
            .sidebar{
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar">
            <div class="app-icon">
                <!-- You can replace "Recipe App" with your preferred app name -->
                <span class="app-name">
                    <img src="/images/appLogo/headerLogo.png" alt="">
                </span>
            </div>
        </nav>
    </header>
    <div class="container">
        <div class="form-container">
            <h2>Edit Dish Recipe</h2>
            <form id="dishForm" action="/editDish/<%= post._id %>" method="post" enctype="multipart/form-data">
                <label for="dishName">Dish Name:</label>
                <input type="text" id="dishName" name="dishName" value="<%= post.dishName %>" required>
                
                <label for="dishCategory">Dish Category:</label>
                <input type="text" id="dishCategory" name="dishCategory" value="<%= post.dishCategory %>" required placeholder="Important! This should be based on the main ingredient you are using in your dish.">
                
                <label for="dishIngredients">Dish Ingredients:</label>
                <textarea id="dishIngredients" name="dishIngredients" rows="4" required placeholder="Important! You have to mention the ingredients followed by a space."><%= post.dishIngredients %></textarea>
                
                <label for="dishInstructions">Dish Instructions:</label>
                <textarea id="dishInstructions" name="dishInstructions" rows="4" required><%= post.dishInstructions %></textarea>
                
                <label for="dishDescription">Dish Description:</label>
                <textarea id="dishDescription" name="dishDescription" rows="4" required><%= post.dishDescription %></textarea>
                
                <label for="dishYouTubeLink">Dish YouTube Link:</label>
                <input type="url" id="dishYouTubeLink" name="dishYouTubeLink" value="<%= post.dishYouTubeLink %>" required>
                
                <div class="current-image">
                    <p>Current Image:</p>
                    <img src="/images/uploads/<%= post.filename %>" alt="Current Dish Image">
                </div>
                
                <label for="dishImage">Upload New Dish Image (optional):</label>
                <input type="file" id="dishImage" name="dishImage" accept="image/*">
                
                <button type="submit">Edit Post</button>
            </form>
        </div>
        <div class="sidebar">
            <button onclick="document.getElementById('dishForm').submit();">Edit Post</button>
            <button onclick="window.location.href='/account/postcreated';">Created Posts</button>
        </div>
    </div>
</body>
</html>
