<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Posts Page</title>
    <link rel="icon" type="image/png" href="./images/appLogo/fav.png">
    <link rel="stylesheet" href="/css/header.css">
    <style>
        /* General styles, reset, and layout */
        body, h1, p, ul, li, a {
            margin: 0;
            padding: 0;
            list-style: none;
            text-decoration: none;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            color: #fff;
            background-color: #121212;
            padding-top: 70px;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            padding: 15px;
        }

        .profile-container {
            flex: 1;
            padding: 1.5rem;
            background-color: #1a1a1a;
            border-radius: 12px;
            margin-bottom: 2rem;
            border: 2px solid #20f803;
            box-shadow: 0 0 15px rgba(32, 248, 3, 0.2);
        }

        .profile-container img {
            height: 60px;
            width: 60px;
            border-radius: 100%;
            border: 4px solid #20f803;
            padding: 5px;
            cursor: pointer;
            margin-bottom: 10px;
        }

        .profile-container h2 {
            color: #20f803;
            margin-bottom: 8px;
            font-size: 1.5rem;
        }

        .profile-container p {
            color: #bfbfbf;
            margin-bottom: 5px;
        }

        /* Modern Card Styles */
        .posts {
            display: flex;
            flex-wrap: wrap;
            gap: 25px;
            justify-content: center;
            padding: 20px;
            margin-top: 1rem;
        }

        .userfoodcard {
            width: 300px;
            background: #1a1a1a;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
            transition: all 0.3s ease;
            border: 2px solid #333;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        .userfoodcard:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 25px rgba(32, 248, 3, 0.25);
            border-color: #20f803;
        }

        .card-image-container {
            position: relative;
            height: 200px;
            overflow: hidden;
        }

        .userfoodcard img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .userfoodcard:hover img {
            transform: scale(1.05);
        }

        .category-badge {
            position: absolute;
            top: 12px;
            left: 12px;
            background: rgba(32, 248, 3, 0.9);
            color: #000;
            font-size: 0.75rem;
            font-weight: bold;
            padding: 6px 14px;
            border-radius: 20px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
        }

        .card-content {
            padding: 1.5rem;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }

        .card-title {
            font-size: 1.3rem;
            color: #20f803;
            font-weight: bold;
            margin: 0;
            line-height: 1.3;
            flex: 1;
            margin-right: 10px;
            text-shadow: 0 0 5px rgba(32, 248, 3, 0.3);
        }

        .likes-count {
            display: flex;
            align-items: center;
            background: rgba(255, 255, 255, 0.1);
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            color: #ddd;
            white-space: nowrap;
        }

        .likes-count svg {
            margin-right: 5px;
            fill: #ff4757;
        }

        .card-description {
            color: #bbb;
            font-size: 0.9rem;
            line-height: 1.5;
            margin-bottom: 1.5rem;
            flex-grow: 1;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .view-recipe-btn {
            background: #20f803;
            color: #000;
            font-weight: bold;
            padding: 10px 20px;
            border-radius: 8px;
            transition: all 0.3s ease;
            text-align: center;
            flex: 1;
            margin-right: 10px;
            text-decoration: none;
            box-shadow: 0 4px 10px rgba(32, 248, 3, 0.3);
        }

        .view-recipe-btn:hover {
            background: #1bd002;
            box-shadow: 0 6px 15px rgba(32, 248, 3, 0.4);
            transform: translateY(-2px);
        }

        .save-btn {
            background: rgba(255, 255, 255, 0.1);
            color: #ddd;
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .save-btn:hover {
            background: rgba(32, 248, 3, 0.2);
            color: #20f803;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #666;
            width: 100%;
        }

        .empty-state h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #aaa;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                padding: 1rem;
            }

            .profile-container {
                margin-right: 0;
                margin-bottom: 2rem;
            }

            .posts {
                padding: 1rem;
                gap: 20px;
            }
            
            .userfoodcard {
                width: 100%;
                max-width: 350px;
            }
        }

        @media (max-width: 480px) {
            .posts {
                padding: 0.5rem;
            }
            
            .card-content {
                padding: 1.2rem;
            }
            
            .card-title {
                font-size: 1.2rem;
            }
            
            .profile-container {
                padding: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <%- include("./header") %>
    
    <div class="container" style="display: block;">
        <div class="profile-container">
            <img src="<%= user.profilePic %>" id="profileImage" alt="Profile Picture">
            <h2><%= user.name %></h2>
            <p>Hi I am <%= user.name %></p>
            <p>Here you can see all the dishes I've shared.</p>
        </div>
    </div>

    <div class="posts">
        <% if (user.post && user.post.length > 0) { %>
            <% user.post.forEach(e=>{ %>
                <div class="userfoodcard">
                    <div class="card-image-container">
                        <img src="<%= e.strMealThumb %>" alt="<%= e.strMeal %>">
                        <div class="category-badge"><%= e.strCategory %></div>
                    </div>
                    <div class="card-content">
                        <div class="card-header">
                            <h3 class="card-title"><%= e.strMeal %></h3>
                            <div class="likes-count">
                                <svg width="16" height="16" viewBox="0 0 24 24">
                                    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                                </svg>
                                <%= e.total_likes.length %>
                            </div>
                        </div>
                        <p class="card-description"><%= e.dishDescription %></p>
                        <div class="card-footer">
                            <a href="/home/recipe/item/<%= e._id %>" class="view-recipe-btn">View Recipe</a>
                            <button class="save-btn">
                                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"></path>
                                </svg>
                            </button>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="empty-state">
                <h3>No recipes yet</h3>
                <p>Start sharing your delicious recipes with the community!</p>
            </div>
        <% } %>
    </div>

    <script>
        // Save button functionality
        document.querySelectorAll('.save-btn').forEach(button => {
            button.addEventListener('click', function() {
                this.classList.toggle('saved');
                if (this.classList.contains('saved')) {
                    this.innerHTML = `
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="#20f803" stroke="#20f803" stroke-width="2">
                            <path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"></path>
                        </svg>
                    `;
                } else {
                    this.innerHTML = `
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"></path>
                        </svg>
                    `;
                }
            });
        });
    </script>
</body>
</html>